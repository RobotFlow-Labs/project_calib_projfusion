# Task PRD-0102: Implement SE3 And Projection Utilities

> PRD: PRD-01 | Depends: PRD-0101
> Files: 3 | Lines: ~320 | Time: ~90min

## What to Build
Port the calibration math primitives needed everywhere else: SE(3) exponential/logarithm, transform composition, and point projection into the image-feature plane.

## Files

### Create: `src/anima_calib_projfusion/geometry/se3.py`
```python
def se3_exp(xi):
    ...


def se3_log(T):
    ...
```

### Create: `src/anima_calib_projfusion/geometry/projection.py`
```python
def project_points(xyz_cam, camera_info, feature_hw):
    ...


def normalize_grid(uv, feature_hw):
    ...
```

### Create: `tests/test_geometry_se3.py`
```python
def test_se3_exp_log_roundtrip():
    ...
```

## Verify
```bash
uv run pytest tests/test_geometry_se3.py tests/test_projection.py -v
```
