# Task PRD-0103: Encode Dataset Splits And Perturbation Sampling

> PRD: PRD-01 | Depends: PRD-0102
> Files: 4 | Lines: ~260 | Time: ~75min

## What to Build
Encode the paper’s KITTI and nuScenes splits plus perturbation ranges so all later training and evaluation code uses a single source of truth.

## Files

### Create: `src/anima_calib_projfusion/data/splits.py`
```python
KITTI_TRAIN = ["00", "02", "03", "04", "05", "06", "07", "08", "10", "12", "21"]
KITTI_VAL = ["11", "17", "20"]
KITTI_TEST = ["13", "14", "15", "16", "18"]
```

### Create: `src/anima_calib_projfusion/data/perturbation.py`
```python
PERTURBATION_RANGES = ((15.0, 0.15), (10.0, 0.25), (10.0, 0.50))
```

### Create: `tests/test_data_splits.py`
```python
def test_kitti_split_lengths():
    ...
```

## Verify
```bash
uv run pytest tests/test_data_splits.py tests/test_config.py -v
```
