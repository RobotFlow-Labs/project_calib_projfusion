# Task PRD-0202: Build Positional Encoding And Cross-Attention

> PRD: PRD-02 | Depends: PRD-0201
> Files: 3 | Lines: ~320 | Time: ~90min

## What to Build
Implement harmonic positional encoding plus the scale-free cross-attention block used to fuse image and point tokens.

## Files

### Create: `src/anima_calib_projfusion/model/positional_encoding.py`
```python
class HarmonicEmbedding(nn.Module):
    def forward(self, xy):
        return encoded_xy
```

### Create: `src/anima_calib_projfusion/model/cross_attention.py`
```python
class ScaleFreeCrossAttention(nn.Module):
    def forward(self, image_tokens, point_tokens, point_uv):
        return fused_tokens
```

### Create: `tests/test_positional_encoding.py`
```python
def test_harmonic_embedding_output_dim():
    ...
```

## Verify
```bash
uv run pytest tests/test_positional_encoding.py -v
```
