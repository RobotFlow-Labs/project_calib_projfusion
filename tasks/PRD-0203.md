# Task PRD-0203: Assemble Dual-Branch ProjFusion

> PRD: PRD-02 | Depends: PRD-0202
> Files: 4 | Lines: ~360 | Time: ~90min

## What to Build
Wire the encoders, coordinate alignment, dual attention blocks, aggregation, and MLP heads into the `ProjDualFusion` model used by the paper.

## Files

### Create: `src/anima_calib_projfusion/model/projfusion.py`
```python
class ProjDualFusion(nn.Module):
    def forward(self, img, pcd, init_extrinsic, camera_info):
        return rot_log, tsl_log
```

### Create: `src/anima_calib_projfusion/model/aggregation.py`
```python
class MiniResAggregation(nn.Module):
    ...
```

### Create: `src/anima_calib_projfusion/model/heads.py`
```python
class RegressionHead(nn.Module):
    ...
```

### Create: `tests/test_projfusion_forward.py`
```python
def test_projfusion_forward_contract():
    ...
```

## Verify
```bash
uv run pytest tests/test_projfusion_forward.py -v
```
