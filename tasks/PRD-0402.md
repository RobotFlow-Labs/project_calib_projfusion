# Task PRD-0402: Build Benchmark Runner

> PRD: PRD-04 | Depends: PRD-0401
> Files: 2 | Lines: ~240 | Time: ~75min

## What to Build
Create the benchmark loop that evaluates a checkpoint across KITTI and nuScenes perturbation ranges and aggregates metrics by split.

## Files

### Create: `src/anima_calib_projfusion/eval/benchmark.py`
```python
class BenchmarkRunner:
    def run_range(self, dataset_id, perturbation_range):
        ...
```

### Create: `tests/test_benchmark_runner.py`
```python
def test_runner_returns_metric_bundle():
    ...
```

## Verify
```bash
uv run pytest tests/test_benchmark_runner.py -v
```
