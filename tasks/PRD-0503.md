# Task PRD-0503: Package Docker Runtime

> PRD: PRD-05 | Depends: PRD-0502
> Files: 2 | Lines: ~140 | Time: ~45min

## What to Build
Create a Docker image and compose file for local API execution and checkpoint-mounted inference.

## Files

### Create: `docker/Dockerfile`
```dockerfile
FROM python:3.10-slim
```

### Create: `docker/compose.yaml`
```yaml
services:
  calib-projfusion:
    build: .
```

## Verify
```bash
uv run pytest tests/test_api_app.py tests/test_service_contract.py -v
```
