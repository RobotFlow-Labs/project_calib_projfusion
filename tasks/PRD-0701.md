# Task PRD-0701: Export Release Bundle

> PRD: PRD-07 | Depends: PRD-0403
> Files: 1 | Lines: ~160 | Time: ~45min

## What to Build
Package checkpoint, config, evaluation report, and manifest into a versioned release bundle for downstream ANIMA consumers.

## Files

### Create: `scripts/export_release.py`
```python
def export_release(checkpoint_path, config_path, report_path):
    ...
```

## Verify
```bash
uv run pytest tests/test_release_bundle.py -v
```
